export function initCount(){
    var count = 0;
    function autoGeneratedNumber(){
        count++;
        return count;
    }
    return autoGeneratedNumber;/// Fn + Lexical Scope = Closure
}